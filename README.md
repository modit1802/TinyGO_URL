# TinyGoURL - URL Shortener

TinyGoURL is a simple URL shortener built with Go (Golang). It lets you shorten long URLs and redirect them back to the original address using a clean, easy-to-use REST API.

## Features

- Shorten any long URL into a small, easy-to-share link.
- Redirect back to the original URL when you visit the shortened one.
